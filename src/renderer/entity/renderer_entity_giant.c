#include <renderer/entity/renderer_entity_giant.h>

renderer_entity_t renderer_entity_giant_create(model_t *model, float shadow_size, float scale) {
    renderer_entity_t renderer = renderer_entity_create();

    return renderer;
}