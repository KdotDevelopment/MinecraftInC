#include <world/block/block_container.h>

#include <world/material/material.h>

block_t block_container_create(uint8_t id, uint8_t texture_id, material_t material) {
    //block_t block = block_create(id, texture_id);
    
    //return block;
}