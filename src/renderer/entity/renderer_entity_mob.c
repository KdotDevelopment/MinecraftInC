#include <renderer/entity/renderer_entity_mob.h>

renderer_entity_t renderer_entity_mob_create(model_t *model, float shadow_size) {
    renderer_entity_t renderer = renderer_entity_create();

    return renderer;
}